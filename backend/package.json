{
  "name": "vintage-closet-backend",
  "version": "1.0.0",
  "description": "Backend automation for vintage closet inventory management",
  "main": "api/stripe-webhook.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test-webhook": "stripe listen --forward-to localhost:3000/api/stripe-webhook"
  },
  "dependencies": {
    "stripe": "^14.0.0",
    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  }
}
